export type OfficeHours={opens_at:string|null;closes_at:string|null;is_closed:boolean;is_24h:boolean;break_starts_at?:string|null;break_ends_at?:string|null};
export function minutes(time:string){const [h,m]=time.split(":").map(Number);return h*60+m}
export function validDate(value:string){if(!/^\d{4}-\d{2}-\d{2}$/.test(value))return false;const d=new Date(value+"T12:00:00Z");return Number.isFinite(d.getTime())&&d.toISOString().slice(0,10)===value}
export function validTime(value:string){return /^([01]\d|2[0-3]):[0-5]\d(:00)?$/.test(value)}
export function businessNow(timezone:string,now=new Date()){const parts=new Intl.DateTimeFormat("en-CA",{timeZone:timezone,year:"numeric",month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",hourCycle:"h23"}).formatToParts(now);const get=(type:string)=>parts.find(p=>p.type===type)?.value||"";return {date:`${get("year")}-${get("month")}-${get("day")}`,minutes:Number(get("hour"))*60+Number(get("minute"))}}
export function fitsHours(hours:OfficeHours,start:number,duration:number){if(hours.is_closed||!Number.isFinite(duration)||duration<=0)return false;const open=hours.is_24h?0:hours.opens_at?minutes(hours.opens_at):NaN;const close=hours.is_24h?1440:hours.closes_at?minutes(hours.closes_at):NaN;if(!(start>=open&&start+duration<=close))return false;return !(hours.break_starts_at&&hours.break_ends_at&&start<minutes(hours.break_ends_at)&&start+duration>minutes(hours.break_starts_at))}
export function overlaps(start:number,duration:number,booked:string[]){return booked.some(t=>start<minutes(t)+duration&&start+duration>minutes(t))}
